info:
  name: Logout
  type: http
  seq: 3

http:
  method: POST
  url: "{{api_url}}auth/logout"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setGlobalEnvVar("auth_token", "")

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
