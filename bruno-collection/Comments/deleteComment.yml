info:
  name: deleteComment
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{api_url}}work/{{work_id}}/comments/{{comment_id}}"
  auth: inherit

runtime:
  variables:
    - name: comment_id
      value: df3d05f8-e140-4d1a-a98d-c95408d7a328

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
