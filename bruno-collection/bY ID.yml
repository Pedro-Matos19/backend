info:
  name: bY ID
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_url}}work/33333333-3333-3333-3333-333333333001"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setGlobalEnvVar("work_id", res.getBody().id)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
