info:
  name: getAllComents
  type: http
  seq: 5

http:
  method: GET
  url: "{{api_url}}admin/comments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
