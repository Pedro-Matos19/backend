info:
  name: getUsers
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_url}}admin?status=pending"
  params:
    - name: status
      value: pending
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
